[package]
name = "re-search-crawler"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "rustls-tls", "gzip", "brotli", "deflate"] }
scraper = "0.19"
url = "2.5"
chrono = "0.4"
rand = "0.8"
